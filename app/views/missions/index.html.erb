<h1>Missions#index</h1>
<p>Find me in app/views/missions/index.html.erb</p>
<ul>
  <% @missions.each do |mission| %>
    <%= link_to mission_path(mission) do %>
      <li><%= mission.name %></li>
      <li><%= mission.company %></li>
    <% end %>
  <% end %>
</ul>
